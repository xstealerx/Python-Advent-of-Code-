vorname = "Mustermann"
nachname = "Max"

# Zuerst definieren wir uns eine Hilfsvariable und speichern dort z. B. den Wert von "vorname".
tmp = vorname

# Jetzt kopieren wir den Wert von "nachname" in die Variable "vorname".
vorname = nachname

# Zum Schluss kopieren wir den Wert von "tmp" in die Variable "nachname".
nachname = tmp